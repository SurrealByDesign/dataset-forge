[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dataset-forge"
version = "1.3.0"
description = "Dataset Forge v1.3.0 -- a read-only LoRA dataset curation workstation with advisory caption/metadata consistency inspection, image encoding context, exact duplicate detection, policy-aware sidecar semantics, an internal Inspection Profile contract, review signal policy resolution, Analyzer Descriptors, Dataset Intelligence, manifest-aware comparison, Inspection Manifest provenance, local Review Desk, inspect, review, improvement planning, and preview workflows."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = [
    "Pillow>=10.0",
    "opencv-python>=4.10",
]

[project.scripts]
dataset-forge = "dataset_forge.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
dataset_forge = ["config/*.json"]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
